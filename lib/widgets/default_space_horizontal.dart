import 'package:projects/common/colors.dart';
import 'package:projects/common/dimensions.dart';
import 'package:flutter/material.dart';

class DefaultSpaceHorizontal extends StatelessWidget {
  const DefaultSpaceHorizontal({super.key});
  @override
  Widget build(BuildContext context) {
    return const SizedBox(width: DEFAULT_SPACING);
  }
}
