import 'package:projects/common/dimensions.dart';
import 'package:flutter/material.dart';

class DefaultSpaceVertical extends StatelessWidget {
  const DefaultSpaceVertical({super.key});
  @override
  Widget build(BuildContext context) {
    return  const SizedBox(height: DEFAULT_SPACING);
  }
}
